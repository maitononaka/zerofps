# Zero Division V35

M4A1 first-person view now uses `assets/m4a1_config.json` as the authoritative camera + socket configuration.
The legacy `zdv3` folder has been removed.

Run with Live Server or `python -m http.server 8000`.
